version: '2'

services:
  redis:
    restart: always
    image: redis:latest
    command:
    - --loglevel warning
    ports:
    - "10022:80"
    volumes:
    - /srv/docker/gitlab/redis:/var/lib/redis:Z
